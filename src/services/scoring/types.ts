/**
 * Canonical scoring types live in `@/scoring`. Re-export for existing import paths.
 */
export type {
  ConfidenceResult as ConfidenceScore,
  ScoreBreakdown,
  ScoringEngineInput,
  ScenarioPatch,
  UserAssumptionOverrides,
} from '@/scoring';
