/**
 * Re-export RevenueCat entitlement id for backwards compatibility with `entitlementLogic` and docs.
 * Configure the same identifier in the RevenueCat dashboard.
 */
export { RC_ENTITLEMENT_PRO as PREMIUM_ENTITLEMENT_ID } from '@/services/revenuecat/productIds';
