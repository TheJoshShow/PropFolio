/**
 * Icon sizing tokens for PropFolio.
 * Keep these in sync with typography so headers feel balanced.
 */

export const iconSizes = {
  xs: 14,
  sm: 18,
  md: 22,
  lg: 28,
  xl: 34,
} as const;

