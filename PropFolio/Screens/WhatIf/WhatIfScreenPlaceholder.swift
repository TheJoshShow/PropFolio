//
//  WhatIfScreenPlaceholder.swift
//  PropFolio
//
//  Placeholder: delete once real what-if screen exists.
//  Add: sliders for key inputs (price, rent, vacancy, expenses, rate, term);
//  real-time metric updates; optional save scenario. Loading/success/error.
//

import SwiftUI

struct WhatIfScreenPlaceholder: View {
    var body: some View {
        Text("What-If")
            .font(.headline)
    }
}
